interface:
  display_name: "Notification Repo Map"
  short_description: "按 URL、MF、包名、接口或业务线索定位通知/邮件多仓"
  default_prompt: "Use $notification-repo-map to look up the relevant notification/email repos, verify local paths, and prepare fork-first work."

policy:
  allow_implicit_invocation: true
